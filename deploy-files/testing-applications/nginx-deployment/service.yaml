apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    app: nginx
spec:
  selector:
    app: nginx
  ports:
    - port: 80
      name: httpPort
      targetPort: http
    - port: 9113
      name: metricsPort
      targetPort: metrics
